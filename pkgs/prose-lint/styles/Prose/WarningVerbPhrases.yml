extends: substitution
message: "%s is more specific than '%s'."
level: warning
ignorecase: true
swap:
  lands on: "'goes to'"
  reaches for: "'uses' or 'imports'"
  serves? both: "'is used by both'"
  survives as: "'remains' or 'becomes'"
