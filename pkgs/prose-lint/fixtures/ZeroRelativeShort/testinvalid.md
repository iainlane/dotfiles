Everything the repository declares is listed here.

Toolsets every platform gets, whatever its role.

Options a service exposes to the module that imports it.

Files the host would export are listed here.
