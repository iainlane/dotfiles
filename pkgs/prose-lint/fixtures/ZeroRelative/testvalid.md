Config schema 39 to 44 removes no key that this repository sets. Every file that
git does not track counts as added. Which secrets a machine needs follows from
this.

The host would export the files.
