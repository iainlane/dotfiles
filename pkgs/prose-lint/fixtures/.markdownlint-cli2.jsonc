// Each fixture file is the smallest input that makes one rule report, or the
// smallest input that rule must not report. They are not documents, so the
// markdownlint rules about document structure and line length do not apply.
{
	"default": true,
	"config": {
		"MD013": false,
		"MD041": false
	}
}
