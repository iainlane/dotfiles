return {
  {
    "LazyVim/LazyVim",

    -- Follow `main`. LazyVim pins itself to `version = "*"` (latest semver
    -- tag) by default.
    version = false,
    branch = "main",

    opts = {
      colorscheme = "catppuccin",
    },
  },
}
